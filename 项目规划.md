# MATLAB Refactor Agent 项目规划

> 更新日期：2026-08-13
> 当前阶段：CLI 完整闭环已实现，进入准确性、运行时验证和工程化增强阶段。

## 项目目标

输入一个 MATLAB 项目目录，建立可靠的代码事实索引，在人工可控的前提下生成并执行重构计划，最终交付隔离的新代码库、验证结果和自然语言报告。

核心原则：

- 输入项目始终只读；
- 确定性事实与 LLM 推理分离；
- LLM 只生成结构化语义或候选建议；
- 安全关键决策由代码校验并由人审批；
- 所有阶段可审计、可恢复、可重放；
- 修复产生新版本，不覆盖历史输出。

## 已完成功能

### 1. CLI 与配置

- [x] `scan`、`analyze`、`annotate`、`plan`、`review`、`report`、`status` 命令。
- [x] console script 与 `python -m matlab_refactor_agent` 双入口。
- [x] `.env`、进程环境变量和默认值配置合并；移除 YAML 配置入口。
- [x] 输入、代码输出、报告、图、artifact、状态库和 checkpoint 路径配置。
- [x] Rich 终端展示和 Pydantic JSON 输出。
- [x] Job ID、Worker 状态和跨进程报告查询。

### 2. MATLAB 确定性预处理

- [x] 递归发现 `.m` 文件和可配置排除规则。
- [x] maxx/Tree-sitter 解析适配与保守降级提取。
- [x] 函数、脚本、package、局部函数、输入输出、行号和调用名称提取。
- [x] manifest 固定输入文件集合。
- [x] 动态分块解析、并行 fan-out 和完整性 fan-in。
- [x] 调用名称作用域消歧和未解析调用记录。
- [x] 调用图、SCC 循环簇、少量代表性环、孤立对象、入口点和未解析调用分析。
- [x] 结合函数源码与依赖邻居，由 AI 语义判断项目的重要算法核心。
- [x] Rich 依赖树、Graph JSON 和 Mermaid `.mmd` 输出。
- [x] 确定性能力统一收敛到 `workers`，移除重复 `capabilities` 层。

### 3. LangGraph 编排与持久化

- [x] 统一 StateGraph 覆盖扫描到报告的完整流程。
- [x] SQLite checkpoint 和中断恢复。
- [x] SQLite/WAL Job 与 Task 审计投影。
- [x] Worker 类型路由、质量门禁和路径冲突检测。
- [x] 解析分块与语义工作单元动态并行。
- [x] 人工审批和修复复审 interrupt。
- [x] 大型数据 artifact 化，Graph State 仅保存轻量引用。

### 4. Multi-Agent 语义与规划

- [x] OpenAI-compatible 结构化 LLM 客户端。
- [x] SDK/API 重试和 JSON/Pydantic 响应重试。
- [x] API Key 只从指定环境变量读取。
- [x] SCC 保序函数簇、token 预算和有限源码上下文。
- [x] 函数、文件、项目三级语义索引。
- [x] 源码证据哈希、低置信度和语义冲突记录。
- [x] ModuleResponsibilityAgent 模块职责规划。
- [x] NamingDirectoryAgent 命名和目录规划。
- [x] PlanReconciler 完整映射、路径、标识符和移动冲突检查。

### 5. 隔离执行、验证与报告

- [x] 输入树执行前后哈希校验。
- [x] 临时 staging 复制和排除规则。
- [x] MATLAB 标识符及引用确定性改写。
- [x] 分阶段文件移动，避免交换路径覆盖。
- [x] 原子发布隔离输出目录。
- [x] ChangeSet 标记、计划哈希和输出树哈希。
- [x] 输出代码库重新解析和调用图回归。
- [x] 源完整性、输出完整性、解析、计划符合性和依赖图检查。
- [x] RepairAgent、人工复审和有限次数隔离重放。
- [x] NaturalLanguageReportAgent JSON/Markdown 报告。
- [x] 工具验证状态不可被 LLM 改写。

### 6. 测试

- [x] 扫描、解析、依赖图和可视化单元测试。
- [x] 配置与 LLM 客户端单元测试。
- [x] WorkerPool、冲突、计划仲裁和 ChangeSet 单元测试。
- [x] CLI 与 Orchestrator 集成测试。
- [x] 语义、规划、审批、执行、验证、修复和报告闭环测试。

## 近期计划：提高重构准确性

### P0：MATLAB 语法覆盖

- [ ] 完善复杂 `classdef`、methods、properties 和继承关系解析。
- [ ] 支持命令式调用、`feval`、函数句柄和动态调用的保守建模。
- [ ] 提升 package、private、class method 和同名函数的作用域消歧。
- [ ] 区分 MATLAB 内置函数、工具箱函数、第三方函数和真正未解析调用。
- [ ] 增加异常语法、超大文件和混合编码 fixture。

验收标准：复杂 MATLAB 样例不会产生错误内部依赖边；无法确定的调用必须明确标记为 unresolved。

### P0：安全改写

- [ ] 从正则边界替换升级为语法节点级标识符改写。
- [ ] 覆盖 class method、package 限定调用和函数句柄引用。
- [ ] 支持在不改变字符串和注释的前提下注入或更新文档注释。
- [ ] 生成可审阅的逐文件 diff artifact。
- [ ] 为每种 RefactorOperation 增加属性测试和逆向冲突测试。

验收标准：所有支持的引用类型均能随符号移动更新；不修改注释、字符串或无关同名文本。

### P1：语义质量与可重复性

- [ ] 建立带人工基准的 MATLAB 语义评测集。
- [ ] 对提示词、模型、参数和 schema 进行版本化。
- [ ] 按源码哈希缓存语义结果，避免重复调用。
- [ ] 统计 token、延迟和费用。
- [ ] 增加低置信度结果的人工复核接口。
- [ ] 增强模块方案和命名方案之间的交叉约束。

验收标准：相同输入、相同提示版本能够复用结果；语义质量可通过固定评测集量化。

## 下一阶段：运行时验证

### P1：MATLAB Engine 与项目测试

- [ ] 接入 MATLAB Engine for Python。
- [ ] 支持用户配置项目初始化、测试和清理命令。
- [ ] 在隔离环境执行 MATLAB 单元测试。
- [ ] 比较重构前后的数值输出、容差和异常。
- [ ] 记录执行时间、内存和性能回归。
- [ ] 对不可用、跳过、失败和通过保持四态语义。

验收标准：用户可以配置一组基线任务，系统对源项目和输出项目执行相同测试并生成可审计对比。

### P1：恢复与可运维性

- [ ] 增加结构化事件流和阶段耗时指标。
- [ ] 提供损坏 checkpoint/artifact 的诊断工具。
- [ ] 增加 Job 取消、清理和保留策略。
- [ ] 为 LLM 限流、熔断和多供应商路由提供策略层。
- [ ] 评估生产环境 Postgres checkpointer。

## 中长期计划：产品化

### P2：API 与 Web 审批

- [ ] REST API：上传/引用项目、启动 Job、查询状态和下载结果。
- [ ] Web 审批：计划、冲突、diff 和修复建议审阅。
- [ ] 可交互调用图、代码树和函数语义详情。
- [ ] 任务事件流和进度展示。
- [ ] 身份认证、项目隔离和权限控制。

### P2：部署与安全

- [ ] Docker 镜像与可复现构建。
- [ ] 上传项目的沙箱、大小限制和恶意文件防护。
- [ ] 密钥托管、日志脱敏和审计导出。
- [ ] 许可证清单和第三方依赖合规检查。
- [ ] Windows/Linux 生产部署文档。

### P3：高级重构能力

- [ ] 基于模块边界拆分大型 MATLAB package/class。
- [ ] 数据流和共享状态分析。
- [ ] 自动识别可测试边界并生成测试骨架。
- [ ] 跨仓库依赖、Simulink 和 MATLAB Project 支持评估。
- [ ] 多方案生成、风险评分和方案对比。

## 当前限制

- 依赖分析以静态显式调用为主，动态调用仍可能进入 unresolved。
- `classdef` 和复杂方法分派尚未完整覆盖。
- 当前改写器适合受支持的函数重命名和文件移动，不应视为通用 MATLAB AST 重写器。
- MATLAB Engine 数值、性能和项目测试尚未正式接入。
- 当前只提供 CLI，没有 REST API 或 Web UI。
- LLM 结果质量依赖模型、提示和输入源码质量，必须保留人工审批。

## 版本里程碑

| 版本 | 目标 | 状态 |
| --- | --- | --- |
| `0.1` | CLI、确定性扫描分析、LangGraph、语义规划、审批、隔离执行、静态验证和报告闭环 | 已完成 |
| `0.2` | MATLAB 语法覆盖、安全 AST 改写、语义评测和缓存 | 规划中 |
| `0.3` | MATLAB Engine、项目测试、数值和性能验证 | 规划中 |
| `0.4` | REST API、Web 审批、可视化 diff 和部署安全 | 规划中 |

每个里程碑应同时满足：功能测试通过、输入只读边界不被破坏、artifact 可审计、失败状态不被降级为成功。
