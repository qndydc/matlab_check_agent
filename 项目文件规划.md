# MATLAB Refactor Agent v2 项目结构与开发计划

> 版本：v2.0 规划稿  
> 更新：2026-08-06  
> 当前状态：第一部分“前期代码处理”已具备 CLI MVP；其余部分按本文分阶段实现。

## 1. 总体目标

系统接收一个 MATLAB 项目目录或上传后的项目工作区，先以确定性工具建立代码结构图，再由多个专责 Agent 分片阅读源码、形成三级语义注解、规划新代码库并生成报告，最后在隔离工作区执行重构和验证。Web 端展示可缩放、可检索、可点击查看注解与使用方法的代码树。

四大部分如下：

1. **前期代码处理**：Worker-1～3，机械扫描、AST 解析、调用图和函数簇准备，不调用 LLM。
2. **Multi-Agent 语义理解与重构**：分片理解源码、生成三级注解、规划模块/命名/目录、生成新代码库及自然语言报告。
3. **后处理验证**：比较重构前后语法、调用关系、测试、输出、性能和风险。
4. **产品与部署**：FastAPI、React 交互代码树、任务状态流、文件夹导入和 Docker 部署。

## 2. 关键架构原则

- Orchestrator 只管理任务、依赖、状态、重试、审批和 artifact，不承担语义判断。
- Worker-1～3 保持确定性；LLM 失败不影响重新使用其分析产物。
- Multi-Agent 按“函数簇”读取有限源码，不把整个代码库塞入一次请求。
- Agent 只生成结构化提议；所有源文件写入由确定性的 `ChangeSetExecutor` 完成。
- 原项目始终只读；重构发生在独立输出工作区，支持快照、dry-run 和回滚。
- 注释、命名、模块和报告共享同一份版本化语义索引，避免各 Agent 重复理解和互相矛盾。
- 每个阶段必须经过 Schema 校验、交叉一致性检查和 QualityGate。
- 云端 LLM 模式可能发送源码片段；必须显式告知用户并支持本地模型、脱敏和禁止外发模式。

## 3. 总体流水线

```text
项目路径 / 上传包
  -> ImportWorkspace（安全导入、只读源区、输出区）
  -> PreprocessingPipeline
       ScannerAgent -> ParserAgent fan-out/fan-in -> AnalyzerAgent
  -> semantic-work-units.json（按 SCC、package、目录和 token 上限切分）
  -> SemanticAnnotationAgent fan-out
       函数级注解 -> 文件级注解 -> 项目级语义索引
  -> ModuleResponsibilityAgent
       模块边界、职责、耦合问题、拆分/合并建议
  -> NamingDirectoryAgent
       名称映射、目标目录、package 与引用更新计划
  -> PlanReconciler（确定性冲突合并）
       refactor-plan.json + comment-plan.json
  -> 人工审批
  -> ChangeSetExecutor（确定性写入新工作区）
  -> ValidationAgent
       结构/语法/调用图/测试/输出/性能对比
  -> NaturalLanguageReportAgent
       新项目 README.md + 重构报告
  -> Web 图、下载包、报告
```

报告 Agent 在语义理解后可先生成草稿，但最终报告必须在验证完成后生成，以免把未执行或验证失败的计划写成既成事实。

## 4. Agent 与确定性组件边界

| 组件 | 类型 | 读取内容 | 核心输出 | 是否写代码 |
| :--- | :--- | :--- | :--- | :---: |
| `ScannerAgent` | 确定性 Worker | 目录树 | `file-manifest.json` | 否 |
| `ParserAgent` | 确定性 Worker | `.m` 文件分片 | `scan-result.json` | 否 |
| `AnalyzerAgent` | 确定性 Worker | 解析模型 | `analysis-result.json`、函数簇 | 否 |
| `SemanticAnnotationAgent` | LLM Multi-Agent | 目标函数簇源码及邻居摘要 | 函数/文件/项目三级注解 | 否 |
| `ModuleResponsibilityAgent` | LLM Agent | 语义索引和依赖子图 | 模块职责与边界方案 | 否 |
| `NamingDirectoryAgent` | LLM Agent | 语义索引、模块方案和 MATLAB 规则 | 重命名、目录和引用计划 | 否 |
| `PlanReconciler` | 确定性组件 | 多 Agent 提议 | 无冲突 `RefactorPlan` | 否 |
| `ChangeSetExecutor` | 确定性组件 | 已审批计划和源工作区 | 全新代码库、变更清单 | 是，仅输出区 |
| `ValidationAgent` | 工具驱动 Agent | 原项目、新项目、基线和日志 | `ValidationResult` | 否 |
| `NaturalLanguageReportAgent` | LLM Agent | 语义索引、实际变更和验证结果 | 新项目 `README.md`、报告 | 是，仅报告文件 |

不建议让注释 Agent 直接编辑源文件。它应输出带源码定位、原文哈希和置信度的 `CommentProposal`，由 Executor 在审批后原子写入，避免并发覆盖和幻觉污染代码。

## 5. 三级语义注解

### 5.1 函数级 `FunctionAnnotation`

- 稳定 symbol ID、文件和行号。
- 功能、输入、输出、数据流、算法步骤。
- 前置条件、单位、维度、边界条件和副作用。
- 内部/外部依赖、典型调用方式和示例。
- 风险、待确认问题、证据行号和置信度。

### 5.2 文件级 `FileAnnotation`

- 文件角色：脚本、函数集合、类或入口。
- 文件内函数协作方式、共享状态和外部资源。
- 推荐文件头注释、使用方法和迁移注意事项。
- 由函数级注解聚合产生，不重复读取整库。

### 5.3 项目级 `ProjectAnnotation`

- 项目目的、入口、主要数据流和运行步骤。
- 模块地图、关键依赖、配置、环境和示例。
- 已知风险、限制和待补测试。
- 作为新项目 `README.md`、Web 总览和规划 Agent 的共同依据。

主要 artifacts：

```text
semantic-work-units.json
function-annotations/<symbol-id>.json
file-annotations/<file-id>.json
semantic-index.json
semantic-conflicts.json
project-annotation.json
comment-plan.json
```

## 6. 建议代码目录

```text
matlab_check_agent/
├── pyproject.toml
├── README.md
├── config.example.yaml
├── 设计文档.md
├── 项目文件规划.md
├── structure.md
├── docker-compose.yml
├── Dockerfile.api
├── Dockerfile.frontend
│
├── src/matlab_refactor_agent/
│   ├── domain/                         # 跨阶段稳定数据契约
│   │   ├── preprocessing.py            # manifest、AST、依赖图模型
│   │   ├── semantics.py                # 三级注解、证据、置信度、冲突
│   │   ├── planning.py                 # 模块、命名、目录、CommentProposal
│   │   ├── changes.py                  # ChangeSet 和原文哈希
│   │   ├── validation.py               # 基线、差异和性能模型
│   │   ├── jobs.py                     # Job、Task、审批和事件
│   │   └── exceptions.py
│   │
│   ├── preprocessing/                  # 第一部分：完全确定性
│   │   ├── scanner/                    # 当前 Worker-1
│   │   ├── parser/                     # 当前 Worker-2，分片解析
│   │   ├── analyzer/                   # 当前 Worker-3，图分析
│   │   ├── clustering/                 # SCC/社区/package/token 分片
│   │   └── pipeline.py
│   │
│   ├── agents/                         # 第二部分：真正的 Multi-Agent 主体
│   │   ├── base.py                     # Agent、上下文包、结构化响应协议
│   │   ├── semantic_annotation/
│   │   │   ├── agent.py                # 分片函数语义理解
│   │   │   ├── prompts.py
│   │   │   ├── context_builder.py      # 源码、邻居摘要、token 预算
│   │   │   ├── aggregator.py           # 函数 -> 文件 -> 项目注解
│   │   │   └── quality_gate.py
│   │   ├── module_responsibility/
│   │   │   ├── agent.py
│   │   │   ├── prompts.py
│   │   │   └── quality_gate.py
│   │   ├── naming_directory/
│   │   │   ├── agent.py
│   │   │   ├── matlab_rules.py         # package、class、private 等约束
│   │   │   ├── prompts.py
│   │   │   └── quality_gate.py
│   │   ├── validation/
│   │   │   ├── agent.py                # 解释工具结果、定位失败原因
│   │   │   └── prompts.py
│   │   └── report/
│   │       ├── agent.py
│   │       ├── prompts.py
│   │       └── readme_builder.py
│   │
│   ├── planning/                       # 确定性计划合并与审批
│   │   ├── reconciler.py               # 合并模块/命名/注释提议
│   │   ├── conflict_detector.py
│   │   ├── plan_validator.py
│   │   └── approvals.py
│   │
│   ├── execution/                      # 只操作隔离输出工作区
│   │   ├── workspace.py
│   │   ├── snapshot.py
│   │   ├── change_set.py
│   │   ├── comment_writer.py
│   │   ├── rename_move.py
│   │   ├── reference_updater.py
│   │   ├── setup_generator.py
│   │   └── executor.py
│   │
│   ├── postprocessing/                 # 第三部分：验证工具链
│   │   ├── syntax_validator.py
│   │   ├── graph_comparator.py
│   │   ├── matlab_engine.py
│   │   ├── test_runner.py
│   │   ├── output_comparator.py
│   │   ├── performance_benchmark.py
│   │   └── pipeline.py
│   │
│   ├── orchestration/                  # 全局状态机与 fan-out/fan-in
│   │   ├── orchestrator.py
│   │   ├── task_queue.py
│   │   ├── worker_pool.py
│   │   ├── state_manager.py
│   │   ├── artifact_store.py
│   │   ├── quality_gate.py
│   │   └── events.py
│   │
│   ├── infrastructure/
│   │   ├── config.py
│   │   ├── llm/                        # OpenAI-compatible、本地模型、重试
│   │   ├── persistence/                # SQLite；规模化后可接 Redis/Postgres
│   │   ├── security/                   # 路径、上传、脱敏、密钥
│   │   └── logging.py
│   │
│   └── interfaces/
│       ├── cli/                        # analyze/annotate/plan/apply/validate
│       └── api/                        # 项目、Job、图、节点、审批、下载
│
├── frontend/                           # 第四部分：React + TypeScript
│   └── src/
│       ├── features/project-import/    # 拖入文件夹/压缩包、服务器路径
│       ├── features/code-graph/        # 缩放、搜索、过滤、布局
│       ├── features/node-inspector/    # 注解、用法、源码位置、调用关系
│       ├── features/semantic-review/   # 低置信度与冲突审核
│       ├── features/refactor-plan/     # diff、审批、风险
│       ├── features/validation/        # 输出与性能对比
│       └── features/reports/
│
├── tests/
│   ├── unit/{preprocessing,agents,planning,execution,postprocessing}/
│   ├── contract/                       # LLM JSON Schema 与 API 契约
│   ├── integration/                    # 阶段流水线
│   ├── e2e/                            # 上传到下载新项目
│   └── fixtures/matlab_projects/
│
├── docs/
│   ├── architecture.md
│   ├── semantic-annotations.md
│   ├── agent-contracts.md
│   ├── api.md
│   ├── deployment.md
│   └── adr/
│
└── var/
    ├── imports/<project-id>/source/    # 只读导入项目
    ├── jobs/<job-id>/artifacts/        # 所有阶段结构化产物
    ├── workspaces/<job-id>/output/     # 重构后的全新代码库
    ├── snapshots/<job-id>/
    ├── reports/<job-id>/
    └── state.db
```

当前代码不应一次性机械搬迁到上述目录。每完成一个阶段，再迁移对应模块并保持兼容导出，避免大规模目录重排破坏现有 28 项测试。

## 7. Orchestrator 状态机

```text
CREATED
 -> IMPORTING
 -> PREPROCESSING
 -> CLUSTERING
 -> SEMANTIC_ANNOTATING
 -> SEMANTIC_REVIEW_REQUIRED（存在低置信度/冲突时）
 -> STRUCTURE_PLANNING
 -> PLAN_RECONCILING
 -> AWAITING_APPROVAL
 -> BUILDING_OUTPUT
 -> VALIDATING
 -> REPORTING
 -> COMPLETED

任一阶段 -> FAILED
VALIDATING -> FIX_PROPOSED -> AWAITING_APPROVAL（限制重试次数）
```

## 8. Web 图契约

代码树不是简单目录树，而是可切换视图的统一图模型：

- `filesystem`：目录、package、文件、类、函数层级。
- `call`：函数调用和循环依赖。
- `module`：Agent 建议及最终采用的模块职责。
- `diff`：重构前后节点和边变化。

节点至少包含：稳定 ID、类型、显示名、源/目标路径、函数/文件/项目注解摘要、使用示例、置信度、风险、源码定位和可用操作。后端提供分页子图和按需邻居接口，前端不一次加载超大项目全部节点。

建议 API：

```text
POST /projects/import
POST /jobs
GET  /jobs/{id}
GET  /jobs/{id}/events
GET  /jobs/{id}/graph?view=call&root=...&depth=2
GET  /jobs/{id}/nodes/{node_id}
GET  /jobs/{id}/annotations/conflicts
GET  /jobs/{id}/plan
POST /jobs/{id}/plan/approve
POST /jobs/{id}/execute
GET  /jobs/{id}/validation
GET  /jobs/{id}/download
```

浏览器不能直接获得任意本地文件夹路径。桌面/本地部署可使用目录选择与批量上传；服务器部署支持压缩包上传或管理员允许的服务端路径，二者必须分别实现安全边界。

## 9. 开发计划与验收条件

### P0：冻结契约与重命名架构（1 个里程碑）

- 保留 Worker-1～3 功能，将其定义为 `PreprocessingPipeline`。
- 新增三级注解、AgentProposal、RefactorPlan、ChangeSet、ValidationResult Schema。
- 定义源码片段、证据行号、原文哈希和 artifact 版本。
- 输出 ADR：Agent 不直接改源项目、报告在验证后定稿。

验收：Schema 可序列化、迁移兼容、现有测试全部通过。

### P1：语义理解 Multi-Agent MVP

- Worker-3 输出函数簇和 token 预算感知的 work units。
- 实现 LLM Client 协议、假客户端、重试、限流和结构化响应校验。
- 实现 `SemanticAnnotationAgent` fan-out/fan-in。
- 聚合函数级、文件级、项目级注解，检测术语和接口含义冲突。
- CLI 增加 `annotate`，支持断点续跑和只重跑失败分片。

验收：fixture 的每个函数/文件均有注解；所有结论带证据和置信度；无源码直接修改。

### P2：结构规划 Multi-Agent

- 实现 `ModuleResponsibilityAgent`。
- 实现 `NamingDirectoryAgent` 和 MATLAB package/class/private 规则。
- 实现确定性 `PlanReconciler`、冲突检测和人工审批。
- 生成可预览的目录树、名称映射、引用变更和注释计划。

验收：计划内路径唯一、无循环移动、符号映射完整；低置信度项目无法自动审批。

### P3：构建全新代码库

- 创建只读 source 和隔离 output 工作区。
- 实现注释写入、重命名、移动、引用更新和 `setupProject.m`。
- 所有变更使用原文哈希防止写错版本。
- 支持 dry-run、逐项 diff、快照、回滚和幂等重放。

验收：原项目哈希不变；output 可重新被 Worker-1～3 完整解析；计划与实际 ChangeSet 一致。

### P4：后处理验证闭环

- 建立重构前基线：入口、固定输入、随机种子、输出和性能。
- 实现语法、图结构、测试、输出数值容差和性能对比。
- ValidationAgent 只解释工具证据并生成修复提议，不绕过审批直接修改。
- 实现最终 README 和自然语言报告。

验收：报告区分“通过、失败、跳过、无法验证”；未安装 MATLAB 时明确降级，不能伪报通过。

### P5：FastAPI 与交互代码树

- 实现导入、Job、事件、图、节点详情、审批、验证和下载 API。
- React 实现可缩放图、增量加载、搜索过滤和节点详情面板。
- 点击函数节点可查看三级注解、使用方法、调用者/被调用者和源码定位。
- 支持原结构、建议结构、最终结构和 diff 四种视图。

验收：大图按需加载；刷新页面可由 REST 重建状态；端到端完成导入、分析、审批、下载。

### P6：Docker 与生产化

- API、前端、Worker 分离容器；开发环境可用单机 compose。
- 挂载 projects/artifacts，配置资源限制、上传限制、健康检查和日志。
- SQLite 用于单机；多实例部署迁移到 Postgres/Redis 队列。
- 增加认证、租户/Job 隔离、密钥管理和数据清理策略。

验收：`docker compose up` 可启动完整流程；任务重启后可恢复；不同项目数据不可互访。

## 10. 当前最合理的下一步

暂不实现文件写入和 Web。先完成 P0 与 P1：定义三级注解 Schema、函数簇工作单元、LLM Client 协议和 SemanticAnnotationAgent 的假客户端闭环。只有语义索引稳定后，命名、目录、模块和 UI 节点详情才有可靠的数据基础。
