# 代码学习

## 总流程
目前 MVP 已完成一条“MATLAB 项目分析 → AI 规划 → 人工审批 → 隔离重构 → 验证报告”的 CLI 闭环。

```text
输入 MATLAB 项目
→ 扫描与分块解析
→ 构建调用图
→ AI 语义分析与重构规划
→ 人工审批
→ 生成隔离的新代码库
→ 重新验证
→ 输出报告
```

1. 配置与入口

- 使用 Conda 管理 Python 环境。
- 使用 `.env` 管理路径、并发和模型配置。
- 使用 `argparse` 实现 `scan`、`analyze`、`annotate`、`plan`、`review`、`report`、`status` 命令。
- 使用 `python-dotenv` 读取 `.env`。
- 使用 Pydantic 校验所有配置和业务数据。

2. 扫描与解析

- 递归发现 MATLAB `.m` 文件。
- 按文件数量分块并行解析。
- 使用 `maxx` 和 Tree-sitter 提取函数、脚本、package、参数、行号及调用关系。
- 将结果保存为 JSON artifact。

3. 依赖分析

- 使用 NetworkX 构建函数调用图。
- 分析循环依赖、入口点、孤立函数、核心函数和未解析调用。
- 使用 Rich 显示终端依赖树。
- 支持输出 Graph JSON 和 Mermaid `.mmd`。

4. AI 语义与规划

- 使用 OpenAI Python SDK 调用 DeepSeek 的 OpenAI-compatible API。
- 生成函数、文件、项目三级语义注解。
- 生成模块职责、函数命名和目录调整建议。
- 使用 Pydantic 强制模型返回结构化 JSON。
- 使用确定性代码合并建议并检查名称和路径冲突。

5. 工作流与人工审批

- 使用 LangGraph 编排完整流程。
- 使用 SQLite checkpoint 保存进度并支持中断恢复。
- `plan` 生成计划后暂停。
- 用户通过 `review` 批准、拒绝或要求修改。

6. 隔离重构

- 使用 Python `shutil`、`pathlib` 和正则处理文件。
- 将原项目复制到独立 staging 目录。
- 在副本中修改函数名、引用和文件路径。
- 使用 SHA-256 检查原项目始终未被修改。
- 完成后原子发布为新的代码库。

7. 验证与报告

- 对输出代码库重新扫描、解析和构建调用图。
- 检查源代码完整性、输出完整性、语法、计划符合性和依赖图变化。
- 验证失败时调用 RepairAgent 提出修复方案，再次等待审批。
- 最终生成 JSON 和 Markdown 报告。

主要技术栈：

| 用途 | API/库 |
|---|---|
| CLI | `argparse`、`Rich` |
| 配置 | `python-dotenv`、`Pydantic` |
| MATLAB 解析 | `maxx`、Tree-sitter |
| 调用图 | `NetworkX` |
| AI | OpenAI Python SDK、DeepSeek API |
| 工作流 | `LangGraph` |
| 持久化 | SQLite |
| 文件重构 | `pathlib`、`shutil`、`hashlib`、`re` |
| 测试 | `pytest` |

目前尚未完成 MATLAB Engine 数值测试、复杂 `classdef`/动态调用解析、Web UI 和 REST API。

## 1.配置入口

## 2.扫描与解析

它的任务可以用一句话概括：
找出项目中的所有 MATLAB 文件，然后把每个文件转换成 Python 能理解的结构化数据。

整个流程是：
MATLAB 项目文件夹
→ 查找所有 .m 文件
→ 排除不需要的目录
→ 生成文件清单 file-manifest.json
→ 把清单分成多个小块
→ 逐个解析 MATLAB 文件
→ 合并解析结果
→ 保存为 scan-result.json

| 分组 | 文件 | 作用 |
|---|---|---|
| 扫描 | `scanning.py`、`scanner_agent.py` | 寻找 `.m` 文件 |
| 解析 | `parser_protocol.py`、`matlab_parser.py`、`parser_agent.py` | 读取 MATLAB 代码 |
| 数据模型 | `domain/models.py` | 规定结果的数据格式 |
| 调度与存储 | `workflow.py`、`artifacts.py` | 分块、执行、合并、保存 |

## 3.依赖分析

依赖分析模块的完整骨架是：
ScanResult
→ 收集 FunctionInfo
→ 建立限定名、简单名和文件内索引
→ 创建 NetworkX DiGraph 节点
→ 解析 calls
→ 创建 DependencyEdge
→ simple_cycles 棪测循环
→ 入度为 0 检测候选入口
→ degree 为 0 检测孤立函数
→ PageRank 选择核心函数
→ unresolved_calls 保存不确定调用
→ AnalysisResult

然后：
AnalysisResult
├── Rich 终端依赖树
├── 完整分析 JSON
├── Graph JSON
└── Mermaid .mmd
如果只记住一句话，可以记住：
dependency_analysis.py 负责计算事实，analyzer_agent.py 负责接入工作流，graph_output.py 负责把事实展示出来。

## 4.AI 语义与规划

在已经知道“有哪些函数、谁调用谁”之后，让大模型理解代码在业务上做什么，再提出模块划分、函数命名和目录调整建议。

完整流程是：

### AI 语义部分：
AnalysisResult
→ SCC 和 token 分组
→ SemanticWorkUnit
→ 有限源码与邻居上下文
→ SemanticAnnotationAgent
→ DeepSeek API
→ Pydantic 响应校验
→ SemanticAnnotationAggregator
→ SemanticIndex

### AI 规划部分：
AnalysisResult + SemanticIndex
├→ ModuleResponsibilityAgent
└→ NamingDirectoryAgent
        ↓
RefactorPlanningCandidates
→ PlanReconciler
→ RefactorPlan
→ 人工审批
AI 负责理解代码并提出候选，PlanReconciler 负责把候选变成合法计划，用户负责决定是否执行。

## 5.工作流与人工审批

## 6.隔离重构

## 7.验证与报告