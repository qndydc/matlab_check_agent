# MATLAB代码库智能重构系统 —— 架构设计文档

> **版本**：v6.0 | **更新**：2026-08-06

> **架构演进**：Worker-1～3 组成不调用 LLM 的确定性前处理流水线。真正的 Multi-Agent 主体从语义理解开始，拆分为语义注解、模块职责、命名目录、验证和自然语言报告 Agent；代码写入由确定性 Executor 在隔离输出区完成。完整目录和里程碑见 [`项目文件规划.md`](项目文件规划.md)。


## 1. 项目定位

基于 **ReAct 智能体** 的 MATLAB 代码库自动重构工具。输入杂乱项目，输出结构化、可维护、带注释的标准代码库。

**两种入口**：CLI（批处理/CI）+ Web UI（交互式）

**两种部署**：外网（直连LLM API）/ 内网（本地LLM或代理）


## 2. 核心功能

| 功能 | 说明 |
| :--- | :--- |
| 代码解析 | 读取 `.m` 文件，提取函数名、输入输出、调用关系，生成依赖树 |
| 依赖分析 | 检测循环依赖、识别死代码、计算核心函数 |
| 注释生成 | 为所有函数添加统一格式的标准化注释 |
| 语义重命名 | 将晦涩函数名改为有意义的名称，同步更新所有引用 |
| 目录重组 | 按标准结构（`+Core/`、`+Utils/`、`Applications/`）迁移文件 |
| 路径重构 | 生成统一的 `setupProject.m`，消除硬编码 `addpath` |
| 自动验证 | 运行入口脚本，比对重构前后输出，确保数值一致 |


## 3. 智能体架构（Orchestrator + Multi-Agent + Human-in-the-loop）

系统分为四部分：确定性前处理、语义理解与重构 Multi-Agent、验证后处理、Web 与容器部署。Agent 通过版本化 artifact 协作，不以对话上下文作为全局状态。

```
┌─────────────────────────────────────────────────────────────────┐
│                       ReAct 智能体循环                          │
│                                                                 │
│   ┌──────────┐    ┌──────────┐    ┌──────────┐               │
│   │ 感知环境  │───▶│ 推理规划  │───▶│ 执行行动  │               │
│   │ (Perceive)│    │ (Reason) │    │  (Act)   │               │
│   └──────────┘    └──────────┘    └────┬─────┘               │
│        ▲                                │                      │
│        │        ┌──────────┐           │                      │
│        └────────│ 反思重试 │◀──────────┘                      │
│        (失败)   │ (Reflect)│           │ (成功)              │
│                 └──────────┘           ▼                      │
│                              ┌──────────────────┐            │
│                              │   验证观察        │            │
│                              │ (Observe/Verify) │            │
│                              └──────────────────┘            │
│                                                                 │
│   验证失败 → 错误日志回传LLM → 生成修正方案 → 重新执行        │
│   验证通过 → ✅ 完成                                           │
│   循环上限: 3次 → 超出则停止，标记需人工介入                   │
└─────────────────────────────────────────────────────────────────┘
```

**人机协同**：验证失败后，LLM生成修正建议，**暂停等待用户审核**后再执行，而非全自动循环。


## 4. 技术选型

| 模块 | 选型 | 说明 |
| :--- | :--- | :--- |
| **代码解析** | `maxx` (Tree-sitter) | 纯Python解析.m文件，无需MATLAB |
| **依赖分析** | `NetworkX` | 构建调用图，检测循环依赖 |
| **语义与规划** | OpenAI Compatible API | 支持云端或本地模型；按函数簇分片调用 |
| **文件操作** | `pathlib` + `shutil` | 跨平台文件迁移 |
| **代码验证** | MATLAB Engine (可选) | 运行入口脚本，验证数值一致性 |
| **Web后端** | FastAPI | 异步高性能 |
| **Web前端** | React + Ant Design + vis-network | 专业UI + 交互式依赖图 |
| **配置管理** | `pydantic` + `yaml` | 类型安全的配置 |
| **日志** | `structlog` | 结构化JSON日志 |
| **备份** | `gitpython` | 自动创建重构分支 |


## 5. 系统架构（四层）

```
┌─────────────────────────────────────────────────────────────┐
│  入口层     CLI (argparse)        Web UI (React+FastAPI)  │
├─────────────────────────────────────────────────────────────┤
│  编排层     Orchestrator — 状态机 + ReAct循环控制         │
├─────────────────────────────────────────────────────────────┤
│  能力层     ┌───────┐ ┌───────┐ ┌────────┐ ┌──────────┐  │
│             │解析器  │ │分析器  │ │规划器  │ │执行器    │  │
│             │(maxx) │ │(nx)   │ │(LLM)  │ │(文件)    │  │
│             └───────┘ └───────┘ └────────┘ └──────────┘  │
│             ┌──────────────┐ ┌──────────────────────────┐  │
│             │ 验证器        │ │ 报告生成器               │  │
│             │ (MATLAB可选) │ │ (Jinja2)                 │  │
│             └──────────────┘ └──────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│  存储层     文件系统 (项目快照)    SQLite (历史)          │
│             Redis (会话/缓存, 可选)                        │
└─────────────────────────────────────────────────────────────┘
```


## 6. 数据模型

| 模型 | 关键字段 |
| :--- | :--- |
| `FunctionInfo` | name, file_path, inputs, outputs, calls, called_by, line_count |
| `AnalysisResult` | functions, cycles, orphans, core_functions, entry_points |
| `RefactorPlan` | files_to_move, files_to_rename, new_comments, setup_script |
| `ValidationResult` | passed, errors, diff_report, matlab_log |


## 7. 配置文件结构

```yaml
# config.yaml
project:
  exclude_patterns: [".git", "slprj", "build"]
  entry_points: []  # 自动检测或手动指定

llm:
  api_base: "https://api.deepseek.com/v1"  # 外网
  # api_base: "http://localhost:8000/v1"   # 内网
  api_key: "${LLM_API_KEY}"
  model: "deepseek-chat"
  temperature: 0.3

refactor:
  target_structure:
    core: "+Core"
    utils: "+Utils"
    applications: "Applications"
    tests: "Tests"
  backup_strategy: "git"  # git | zip | none
  max_retry_cycles: 3

matlab:
  engine_path: ""  # 留空则跳过验证
  timeout: 60

web:
  host: "0.0.0.0"
  port: 8080

logging:
  level: "INFO"
  format: "json"
```


## 8. 核心流程

```
1. 用户提交项目路径（CLI参数 / Web上传）
2. Worker-1 发现 .m 文件 → Worker-2 分片并发调用 maxx → 聚合 FunctionInfo 列表
3. Worker-3 使用 NetworkX 构建调用图 → 检测循环依赖、死代码、核心函数
4. SemanticAnnotationAgent 按函数簇阅读源码 → 聚合函数/文件/项目三级注解
5. ModuleResponsibilityAgent 与 NamingDirectoryAgent 生成模块、命名、目录和注释提议
6. 确定性 PlanReconciler 合并冲突 → 用户审批
7. Executor 在隔离输出区构建全新代码库，原项目保持只读
8. ValidationAgent 驱动工具比较语法、调用图、测试、输出和性能
9. NaturalLanguageReportAgent 根据实际变更与验证结果生成 README 和最终报告
10. Web 展示可缩放代码树、节点用法、重构 diff 与验证结果
```


## 9. 部署模式

| 模式 | 配置 | 适用场景 |
| :--- | :--- | :--- |
| 外网 | `llm.api_base` 指向公网API | 云服务器、个人开发者 |
| 内网 | `llm.api_base` 指向本地 vLLM/Ollama | 企业内网、涉密环境 |
| 代理 | 设置 `HTTP_PROXY` 环境变量 | 内网通过代理访问外网API |


## 10. 安全策略

| 策略 | 说明 |
| :--- | :--- |
| API Key | 环境变量注入，不写入配置文件 |
| 代码隐私 | 语义理解需要发送受控源码分片；必须提供明确授权、本地模型、脱敏及禁止外发模式 |
| 备份 | 执行重构前自动创建Git分支或ZIP快照 |
| 回滚 | 支持一键回滚至重构前状态 |
| 认证 (Web) | 支持OAuth2 / Basic Auth / LDAP（可配置） |


## 11. 依赖清单

### Python (核心)
```
maxx>=0.5.0          # MATLAB解析
networkx>=3.0.0      # 图分析
openai>=1.6.0        # LLM客户端
pydantic>=2.5.0      # 数据模型
fastapi>=0.100.0     # Web后端
uvicorn>=0.24.0      # Web服务器
jinja2>=3.1.0        # 报告模板
structlog>=24.1.0    # 日志
gitpython>=3.1.40    # Git备份
pyyaml>=6.0.0        # 配置
rich>=13.0.0         # CLI美化
```

### 前端 (Web UI)
```
react, antd, vis-network, axios, react-use-websocket
```

### 可选
```
MATLAB R2020a+  # 仅用于验证环节，解析已由maxx替代
Redis           # 多Worker会话管理
```


---
